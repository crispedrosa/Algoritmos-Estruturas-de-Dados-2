### **Exercício 4: Ordenando Vetores de Strings**

Implemente uma função para ordenar um vetor de nomes `(char *)` em ordem alfabética, do `A` ao `Z`. Para isso, você precisará usar a função strcmp() da biblioteca <string.h>.